
// https://leetcode-cn.com/problems/reverse-string/

func reverseString (arr: list<string>) -> list<string> {
    var k = arr.size - 1, i = 0

    while k < arr.size / 2 {
        arr.swap(i, arr[k])

        i += 1
        k -= 1
    }

    return arr
}

println reverseString (['h', 'e', 'l', 'l', 'o']) // ['0', 'l', 'l', 'e', 'h']
