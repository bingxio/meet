
fun fibonacci (l: int) -> list {
    var a: list<int> = [1, 1], i = 2
      
    while i < l {
        a[i] = a[i - 1] + a[i - 2]
      
        i += 1
    }
    
    return a
}

println fibonacci (20)
